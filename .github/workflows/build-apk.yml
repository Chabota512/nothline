name: (disabled - superseded by build-android-apk.yml)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use 'Build Android APK' instead."
